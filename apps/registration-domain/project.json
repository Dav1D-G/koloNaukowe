{
  "name": "registration-domain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/registration-domain/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/registration-domain",
        "main": "apps/registration-domain/src/main.ts",
        "tsConfig": "apps/registration-domain/tsconfig.app.json",
        "assets": [
          "apps/registration-domain/src/assets"
        ],
        "webpackConfig": "apps/registration-domain/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "registration-domain:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "registration-domain:build:development"
        },
        "production": {
          "buildTarget": "registration-domain:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/registration-domain/jest.config.ts"
      }
    }
  },
  "tags": []
}
